# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0
maven_group = io.github.zokona
archives_base_name = end-additions

# Dependencies are managed at gradle/libs.versions.toml
